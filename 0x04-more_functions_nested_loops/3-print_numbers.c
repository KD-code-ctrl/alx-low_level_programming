#include "main.h"

/**
 * print_number - prints numbers up to a range
 * Return:0 when successful
 */
void print_numbers(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
		_putchar(i);
	_putchar('\n');
}
