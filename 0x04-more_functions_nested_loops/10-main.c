#include "main.h"

/**
 * main - has a function that prints a triangel
 *
 * Return:0 always
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
