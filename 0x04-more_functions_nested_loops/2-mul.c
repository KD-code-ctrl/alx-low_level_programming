#include "main.h"

/**
 * mul - multiplys two integers
 * @a: is argument passed in.
 * @b: is argument passed in.
 * Return: Result of the two integers
 */
int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
