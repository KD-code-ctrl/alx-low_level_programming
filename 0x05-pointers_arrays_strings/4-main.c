#include "main.h"

/**
 * main - has function print_rev
 * Return:0 when successful
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
